language: python

python:
  -"3.6.5"

install:
  -pip install -r requirements.txt
  -pip install coveralls codecov

script: 
  -pytest --with -- coverage app/tests

after success:
  -coveralls
  -codecov -- token=
